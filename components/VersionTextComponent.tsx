import {Text} from "react-native";
import React from "react";

const VersionTextComponent = ({versionText}: string) => (
    <Text className="text-center font-JakartaMedium text-gray-500">{versionText}</Text>

)

export default VersionTextComponent